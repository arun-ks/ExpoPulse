import { ArrowLeft, Star } from 'lucide-react'
import { useEffect, useState } from 'react'
import { Link } from 'react-router-dom'
import { Brand } from '../components/Brand'
import { supabase } from '../lib/supabase'
type Row={event_name:string;event_slug:string;public_id:string;company_name:string;rating_value:number|null;comment_text:string|null;updated_at:string;can_edit:boolean}
export function MyFeedbackPage(){const [rows,setRows]=useState<Row[]>([]);useEffect(()=>{void supabase.rpc('contributor_my_feedback').then(({data})=>setRows((data??[]) as Row[]))},[]);return <main className="min-h-screen bg-[#f4f7f9]"><header className="border-b bg-white"><div className="mx-auto flex max-w-5xl items-center justify-between px-5 py-4"><Brand/><Link className="btn-secondary" to="/"><ArrowLeft size={17}/>Home</Link></div></header><section className="mx-auto max-w-5xl px-5 py-10"><h1 className="text-3xl font-black text-[#0b2940]">My feedback</h1><p className="mt-2 text-slate-600">Review and update your Event feedback.</p><div className="mt-7 space-y-4">{rows.map(row=><Link className="card block p-5 hover:shadow-md" key={`${row.event_slug}-${row.public_id}`} to={`/e/${row.event_slug}/exhibitors/${row.public_id}`}><p className="text-sm font-bold text-[#ef5b32]">{row.event_name}</p><h2 className="mt-1 text-lg font-black">{row.company_name}</h2>{row.rating_value&&<p className="mt-2 flex gap-1"><Star size={17} className="fill-amber-400 text-amber-400"/>{row.rating_value}/5</p>}{row.comment_text&&<p className="mt-2 text-slate-600"><span className="font-semibold">Latest comment:</span> {row.comment_text}</p>}<p className="mt-3 text-xs font-semibold text-slate-400">{row.can_edit?'Editable':'Submission closed'}</p></Link>)}{rows.length===0&&<div className="card p-8 text-center text-slate-500">You have not submitted feedback yet.</div>}</div></section></main>}
