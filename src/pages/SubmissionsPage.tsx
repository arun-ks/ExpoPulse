import { useEffect, useState } from 'react'
import { supabase } from '../lib/supabase'
type Row={enquiry_id:string;display_name:string;contact_email:string;message:string;created_at:string}
export function SubmissionsPage(){const [rows,setRows]=useState<Row[]>([]);const [error,setError]=useState('');useEffect(()=>{void supabase.rpc('manage_list_enquiries').then(({data,error})=>error?setError(error.message):setRows((data??[]) as Row[]))},[]);return <><p className="text-sm font-bold uppercase tracking-[.16em] text-[#ef5b32]">Contributor enquiries</p><h1 className="mt-2 text-3xl font-black text-[#0b2940]">Advertising submissions</h1>{error&&<p className="mt-5 text-red-700">{error}</p>}<div className="mt-7 space-y-4">{rows.map(row=><article className="card p-5" key={row.enquiry_id}><div className="flex flex-wrap justify-between gap-2"><div><h2 className="font-black">{row.display_name}</h2><a className="text-sm text-[#d94722]" href={`mailto:${row.contact_email}`}>{row.contact_email}</a></div><time className="text-xs text-slate-400">{new Date(row.created_at).toLocaleString()}</time></div><p className="mt-4 whitespace-pre-wrap text-slate-600">{row.message}</p></article>)}{rows.length===0&&<div className="card p-8 text-center text-slate-500">No advertising enquiries.</div>}</div></>}
