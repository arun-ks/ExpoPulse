import { ArrowLeft, Send } from 'lucide-react'
import { useState, type FormEvent } from 'react'
import { Link } from 'react-router-dom'
import { Brand } from '../components/Brand'
import { supabase } from '../lib/supabase'
export function AdvertisingEnquiryPage(){const [email,setEmail]=useState('');const [message,setMessage]=useState('');const [status,setStatus]=useState('');const [busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setStatus('');const {error}=await supabase.rpc('contributor_submit_enquiry',{p_email:email,p_message:message});setStatus(error?error.message:'Enquiry sent.');if(!error)setMessage('');setBusy(false)}return <main className="min-h-screen bg-[#f4f7f9]"><header className="border-b bg-white"><div className="mx-auto flex max-w-3xl items-center justify-between px-5 py-4"><Brand/><Link className="btn-secondary" to="/"><ArrowLeft size={17}/>Home</Link></div></header><section className="mx-auto max-w-3xl px-5 py-10"><h1 className="text-3xl font-black text-[#0b2940]">Advertising enquiry</h1><p className="mt-2 text-slate-600">Tell the ExpoPulse team what you would like to promote.</p><form onSubmit={submit} className="card mt-7 p-6"><label className="block"><span className="label">Contact email</span><input className="field" type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="mt-5 block"><span className="label">Enquiry</span><textarea className="field min-h-36" minLength={10} maxLength={500} required value={message} onChange={e=>setMessage(e.target.value)}/></label>{status&&<p className="mt-4 text-sm">{status}</p>}<button className="btn-primary mt-5" disabled={busy}><Send size={17}/>{busy?'Sending…':'Send enquiry'}</button></form></section></main>}
